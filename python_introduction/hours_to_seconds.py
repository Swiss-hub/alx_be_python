hours = 2
seconds = hours * 3600
# Function to convert hours to seconds
def hours_to_seconds(hours):
    """Convert hours to seconds."""
    return hours * 3600
print(f"{hours} hours is {hours_to_seconds(hours)} seconds.")